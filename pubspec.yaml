name: flutter_devtools_mcp
description: MCP server for Flutter performance analysis via vm_service
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  dart_mcp: ^0.3.0
  vm_service: ^14.0.0
  stream_channel: ^2.1.0
  args: ^2.4.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
